# V10 implementation note

This branch begins the runtime wiring stage after V10 architecture landed in main. The new code is deliberately deterministic and shadow-only so it can be validated before it touches the current Paper qualification path.
